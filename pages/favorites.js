//need to contain favorites
// const favorites = (props) => {
//     return ( 
//         props.favorites.map((item)=>{})
//      );
// }
 
// export default favorites;
export default function favorites(){
    return(
        <h1>Your Favorites:</h1>
    )
}